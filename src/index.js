import '../firebaseConfig'; // Importa Firebase primero para inicializarlo
import { registerRootComponent } from 'expo';
import App from './App';

// Registra el componente App
registerRootComponent(App);